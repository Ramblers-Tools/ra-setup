<?xml version="1.0" encoding="utf-8"?>
<form>
  <fieldset name="setup">
    <field
      name="sub_account"
      type="text"
      label="Sub account"
      description="Name for the new SMTP2GO sub-account"
      filter="string"
      required="true"
    />
    <field
      name="contact_id"
      type="sql"
      label="Who is to be notified of delivery exceptions"
      query="SELECT u.id AS value, c.name AS text FROM #__users AS u INNER JOIN #__contact_details AS c ON c.user_id = u.id UNION SELECT 0, '--Select Contact --' ORDER BY text"
      key_field="value"
      value_field="text"
      default="0"
      required="true"
    />
    <field
      name="sender_registration"
      type="radio"
      label="Sender verification"
      default="domain"
      required="true"
    >
      <option value="email">Register this address as a Verified Single sender email</option>
      <option value="domain">Register this domain as a Verified sender domain</option>
      <option value="both">Register both the email address and the domain</option>
    </field>
  </fieldset>
</form>
